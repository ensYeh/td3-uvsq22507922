<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
          "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
          "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
  <property name="charset" value="UTF-8"/>
  <property name="severity" value="error"/>

  <!-- Limite de longueur de ligne -->
  <module name="LineLength">
    <property name="max" value="100"/>
  </module>

  <!-- Vérifie la présence du header (désactivé pour ton projet étudiant) -->
  <module name="TreeWalker">

    <!-- Noms de classes : PascalCase -->
    <module name="TypeName"/>

    <!-- Noms de méthodes et variables : camelCase -->
    <module name="MethodName"/>
    <module name="ParameterName"/>
    <module name="LocalVariableName"/>

    <!-- Espacements et indentations -->
    <module name="WhitespaceAround"/>
    <module name="WhitespaceAfter"/>
    <module name="EmptyLineSeparator"/>

    <!-- Vérifie les accolades -->
    <module name="LeftCurly"/>
    <module name="RightCurly"/>

    <!-- Imports -->
    <module name="AvoidStarImport"/>
    <module name="UnusedImports"/>
    <module name="ImportOrder">
      <property name="ordered" value="true"/>
      <property name="separated" value="true"/>
    </module>

    <!-- Visibilité des attributs -->
    <module name="VisibilityModifier"/>

    <!-- Vérifie la documentation Javadoc -->
    <module name="JavadocType"/>
    <module name="JavadocMethod"/>
    <module name="JavadocVariable">
      <property name="scope" value="protected"/>
      <property name="excludeScope" value="package"/>
    </module>

    <!-- Vérifie la complexité -->
    <module name="CyclomaticComplexity">
      <property name="max" value="15"/>
    </module>

    <!-- Vérifie la taille des classes -->
    <module name="ClassDataAbstractionCoupling">
      <property name="max" value="20"/>
    </module>

    <!-- Vérifie les constructions de code inutiles -->
    <module name="SimplifyBooleanExpression"/>
    <module name="SimplifyBooleanReturn"/>

    <!-- Vérifie les commentaires TODO -->
    <module name="TodoComment">
      <property name="format" value="TODO"/>
    </module>

  </module>

  <!-- Vérifie la fin de ligne -->
  <module name="NewlineAtEndOfFile"/>
</module>
